#!/usr/bin/env ruby

require_relative '../config/environment.rb'

puts 'Reverse Polish notation (RPN) is a mathematical notation in which every operator follows all of its operands, in contrast to Polish notation (PN), which puts the operator before its operands. It is also known as postfix notation. It does not need any parentheses as long as each operator has a fixed number of operands.'
puts '- From Wikipedia, the free encyclopedia'
puts 'If there are futher questions on the utility of a RPN, the following reference is recommended: https://en.wikipedia.org/wiki/Reverse_Polish_notation.'

ReversePolishCalculator.new.operation
